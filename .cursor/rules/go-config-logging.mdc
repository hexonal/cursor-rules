---
description: 
globs: 
alwaysApply: true
---
# Go 配置管理与日志规范 (v2.1)

> **注意**: 本规范之上存在一份 [全局元规则](mdc:_meta-rules.mdc)，其具有最高解释权。

本规范旨在提供一套完整的、无二义性的 Go 应用配置管理与日志记录标准。

## 1. 配置管理

*   **外部化**: **严禁**在代码中硬编码任何环境相关的配置项和凭证。
*   **结构化**: **必须**定义 `struct` 来映射所有配置项，并在启动时完成解析。
*   **版本控制**: **必须**在代码仓库中提供一份完整的配置文件模板。**严禁**将任何包含生产环境凭证的实际配置文件提交入库。
*   **加载优先级**: **必须**支持从多种来源（默认值、文件、环境变量、命令行）加载配置并定义清晰的覆盖优先级。
*   **敏感信息**: 所有敏感信息**必须**通过 Secrets Management 系统或加密文件进行管理，**严禁**以明文形式出现在配置文件或环境变量中。

## 2. 日志

*   **结构化**: **必须**使用结构化日志库，将所有日志输出为 JSON 格式。推荐使用 `slog`。
*   **输出目标**: **必须**将日志输出到标准输出 (`stdout`) 或标准错误 (`stderr`)。
*   **禁止敏感信息**: **严禁**在任何级别的日志中记录任何形式的敏感信息。
*   **日志级别**: **必须**严格遵循 `DEBUG`, `INFO`, `WARN`, `ERROR`, `FATAL` 的标准定义使用。
*   **强制字段**: 每一条日志记录**必须**包含 `timestamp`, `level`, `message`, `service` 等核心字段。`ERROR` 级别日志**必须**包含 `error` 和 `stack_trace`。
*   **请求日志**: **必须**使用中间件实现统一的入口请求日志，记录请求方法、路径、状态码、处理时长等信息。
