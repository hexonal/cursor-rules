---
description: "Go 包设计与项目结构规范 (v2.0)，旨在提供一套完整的、无二义性的项目组织标准。"
globs: 
alwaysApply: true
---
# Go 包设计与项目结构规范 (v2.0)

> **注意**: 本规范之上存在一份 [全局元规则](mdc:_meta-rules.mdc)，其具有最高解释权。

本规则涵盖了 Go 项目中包的设计原则、依赖管理以及推荐的项目结构，旨在实现高内聚、低耦合、清晰的模块划分。

## 1. 包设计原则

*   **高内聚**: 包内的代码**必须**专注于单一的职责或领域。
*   **低耦合**: 包之间**必须**尽量减少直接依赖。**必须**优先使用接口来定义协约，以实现依赖倒置。
*   **包命名**: **必须**遵循 [Go 代码风格与格式化规范](mdc:go-style.mdc) 中关于包命名的所有条款。
*   **错误处理**: **必须**遵循 [Go 错误处理规范](mdc:go-errors.mdc) 的所有条款。
*   **入口点 (`main` 包)**: `main` 包**必须**尽可能小，其唯一职责是组装和启动应用。所有业务逻辑**必须**委托给 `internal` 或 `pkg` 中的包。

## 2. 标准项目结构 (Project Layout)

### 2.1. 基础目录结构

所有项目**必须**遵循以下标准目录结构：

*   **`/cmd`**: 存放可执行应用的 `main` 包。
*   **`/internal`**: 存放项目私有的应用和库代码。**这是项目绝大部分代码应该存放的位置**。
    *   `/handler` (或 `controller`): 处理外部请求（HTTP/gRPC）。
    *   `/service` (或 `usecase`): 包含核心业务逻辑。
    *   `/repository` (或 `dao`): 数据访问层。
    *   `/model` (或 `entity`): 定义核心业务对象。
    *   `/middleware`: 存放中间件。
    *   `/config`: 存放配置加载逻辑。
*   **`/pkg`**: 存放可以被外部项目安全导入的公共库代码。如果不确定，**必须**优先放在 `/internal`。
*   **`/api`**: 存放 API 契约文件（如 `.proto`, OpenAPI spec）。这是 [API-First 开发模式](mdc:go-idioms.mdc) 的基础。
*   **`/configs`**: 存放配置文件模板。
*   **`/scripts`**: 存放自动化任务脚本。
*   **`/build`**: 存放 CI/CD 相关文件。
*   **`/test`**: 存放大型测试（集成、E2E）。

### 2.2. 内部包组织详细规范

基于 go-zero 的最佳实践，`/internal` 目录**必须**按以下方式组织：

```
internal/
├── config/       # 配置定义与加载
├── handler/      # HTTP/gRPC 处理器
├── logic/        # 业务逻辑（对应 go-zero 的 logic 层）
├── middleware/   # 中间件
├── model/        # 数据模型
├── server/       # 服务器定义
└── svc/          # 服务上下文（ServiceContext）
```

*   **`/internal/svc`**: **必须**包含 `ServiceContext` 结构体，用于依赖注入和服务初始化。
*   **`/internal/logic`**: **必须**将每个业务逻辑封装为独立的 struct，便于测试和维护。
*   **`/internal/handler`**: **仅**包含请求处理和响应逻辑，**严禁**包含业务逻辑。

### 2.3. 接口定义位置

*   **原则**: 接口**必须**定义在使用它的地方，而不是实现它的地方。
*   **例外**: 标准接口（如 `io.Reader`）或框架定义的接口除外。
*   **实践**: 
    ```go
    // 正确：在 service 包中定义 repository 接口
    package service
    
    type UserRepository interface {
        GetUser(ctx context.Context, id int64) (*User, error)
    }
    
    // 错误：在 repository 包中定义接口
    package repository
    
    type UserRepository interface { // 错误位置
        GetUser(ctx context.Context, id int64) (*User, error)
    }
    ```

## 3. 高级项目结构与工程实践

### 3.1. 多模块单体仓库 (Multi-module Mono-repo)

*   **理念**: 在一个代码仓库中管理多个相互独立的 Go module。
*   **适用场景**: 大型项目、框架、包含多个独立组件的复杂系统。
*   **优点**: 强制解耦、独立版本、依赖清晰。
*   **实践**: 使用 `go work` 进行本地开发，并配合自动化脚本统一管理所有模块。

### 3.2. 工具驱动的标准化 (Tool-driven Standardization)

*   **理念**: "工具大于约定和文档"。**必须**通过自动化工具来强制执行项目结构和编码规范。
*   **实践**:
    *   **代码生成**: 使用 `goctl`, `oapi-codegen` 等工具一键生成服务骨架。
    *   **自定义 Linter**: 编写自定义的静态分析工具，集成到 CI 流程。
    *   **项目脚手架**: 开发自己的项目脚手架工具，封装团队的最佳实践。
