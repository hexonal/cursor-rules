---
description: 
globs: 
alwaysApply: true
---
# Go Context 使用规范 (v2.1)

> **注意**: 本规范之上存在一份 [全局元规则](mdc:_meta-rules.mdc)，其具有最高解释权。

本规范旨在提供一套完整的、无二义性的 `context.Context` 使用标准。

## 1. 核心原则

*   **显式传递**: `context.Context` **必须**作为需要它的函数的第一个参数，且命名**必须**为 `ctx`。
*   **禁止存储**: **严禁**将 `context.Context` 作为 `struct` 的字段。
*   **强制接受场景**: 所有处理外部请求、执行 I/O、可能被阻塞或需要超时控制的函数，**必须**接受 `Context`。

## 2. `Context` 的类型与用途

*   **`WithValue` 使用**:
    1.  **严禁**使用 `context.WithValue` 传递函数的核心参数或依赖项。
    2.  `key` **必须**是自定义的、未导出的类型，以避免冲突。
    3.  **允许传递的值**: 仅限于请求ID、追踪ID、认证信息等横切关注点的元数据。
*   **空 `Context`**:
    *   **`context.Background()`**: **必须**仅在调用链的顶层（如 `main` 函数）使用。
    *   **`context.TODO()`**: **必须**仅作为临时占位符使用，并尽快替换为合适的 `Context`。
*   **传递边界**:
    *   **严禁**将请求的 `ctx` 传递给与该请求生命周期无关的后台 Goroutine。**必须**为其创建新的 `context.Background()`。
