---
description: 
globs: 
alwaysApply: true
---
# Go 安全编程规范 (v2.1)

> **注意**: 本规范之上存在一份 [全局元规则](mdc:_meta-rules.mdc)，其具有最高解释权。

本规范旨在提供一套完整的、无二义性的 Go 应用安全标准。

## 1. 输入验证与输出编码

*   **核心原则：永不信任外部输入**: 所有来自信任边界之外的数据**必须**经过严格的验证和净化。
*   **输入验证**: **必须**在业务逻辑处理前，对所有外部输入执行类型、长度、格式的白名单验证。
*   **输出编码**: 在将数据输出到 HTML, SQL, Shell 等上下文时，**必须**进行相应的编码。**严禁**手动拼接 HTML 或 SQL 字符串。

## 2. 依赖与资源安全

*   **漏洞扫描**: **必须**将 `govulncheck` 集成到 CI/CD 流程中，发现中高危漏洞时**必须**阻塞构建。
*   **资源管理**: **必须**为所有外部调用设置超时；**必须**在 API 入口配置速率限制和请求体大小限制；**必须**对文件上传的大小和类型进行限制。

## 3. Web 应用安全

*   **传输层安全**: 所有对外服务**必须**使用 TLS。
*   **HTTP 安全头**: **必须**在所有 HTTP 响应中设置 `HSTS`, `CSP`, `X-Content-Type-Options`, `X-Frame-Options` 等安全头。
*   **认证与授权**: **严禁**明文存储密码，**必须**使用强哈希算法。在每个需要授权的端点，**必须**严格校验用户权限。
*   **Cookie 安全**: **必须**为 Cookie 设置 `Secure`, `HttpOnly`, `SameSite` 属性。

## 4. 敏感信息处理

*   **严禁**在日志、配置文件、环境变量中以明文形式存储、传输或记录任何敏感信息。
*   **必须**最小化敏感信息在内存中的停留时间。
