---
description: "Go 弹性设计规范 (v2.1)，涵盖如何通过组件化、自动化等方式构建高韧性的 Go 应用。"
globs: 
alwaysApply: true
---
# Go 弹性设计规范 (v2.1)

> **注意**: 本规范之上存在一份 [全局元规则](mdc:_meta-rules.mdc)，其具有最高解释权。

本规范旨在提供一套完整的、无二义性的 Go 应用弹性设计标准。弹性能力**必须**作为系统的一等公民进行设计和实现，而非事后补充。

## 1. 核心原则：韧性能力的组件化

*   **强制解耦**: **严禁**在业务逻辑代码中直接实现任何弹性控制逻辑（如超时、重试、限流、熔断）。
*   **强制组件化**: 所有弹性控制逻辑**必须**被封装为标准化的、可复用的组件。这些组件**必须**以**中间件 (Middleware)** 或**装饰器 (Decorator)** 的形式，透明地应用于需要保护的业务逻辑之上。

## 2. 关键弹性模式规范

### 2.1. 链式超时 (Chained Timeout)

*   **强制**: **必须**为所有可能阻塞的操作（特别是网络 I/O）设置超时。
*   **链式原则**: 在微服务调用链中，下游服务的超时时间**必须**严格小于上游服务的超时时间，以防止资源级联耗尽。
*   **实现**: **必须**通过 [`context.WithTimeout`](mdc:go-context.mdc) 或 `context.WithDeadline` 实现，并在调用链中全程传递 `context`。

### 2.2. 限流 (Rate Limiting)

*   **强制**: **必须**在所有面向用户的 API 入口，以及所有对外部依赖的关键调用点，配置限流策略。

### 2.3. 熔断 (Circuit Breaker)

*   **强制**: 对于所有非关键的、或有明确降级方案的下游服务调用，**必须**配置熔断器。
*   **快速失败**: 熔断器在断开（Open）状态下**必须**直接快速失败，**严禁**发送实际请求。

### 2.4. 降载 (Load Shedding)

*   **强制**: **必须**在系统入口处实现自适应降载机制。该机制**必须**基于一个或多个核心健康指标（如 CPU 使用率、请求队列长度），当指标超过预设安全阈值时，**必须**主动丢弃一部分新来的请求。
*   **响应**: 降载时，**必须**返回明确的错误码（如 HTTP 503）和 `Retry-After` 头。

### 2.5. 自动参数校验

*   **强制**: **必须**将请求参数的合法性校验逻辑与业务处理逻辑分离。
*   **实现**: **必须**通过在请求结构体的字段 `tag` 中定义校验规则来实现。框架的入口层**必须**自动解析并执行校验，校验失败则**必须**直接拒绝请求。
