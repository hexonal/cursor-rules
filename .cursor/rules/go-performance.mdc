---
description: 
globs: 
alwaysApply: true
---
# Go 性能优化规范 (v2.1)

> **注意**: 本规范之上存在一份 [全局元规则](mdc:_meta-rules.mdc)，其具有最高解释权。

本规范旨在提供一套完整的、无二义性的 Go 性能优化标准。核心原则是：**先分析，后优化**。

## 1. 性能分析

*   **禁止猜测**: **严禁**在没有 `pprof` 数据支撑的情况下进行任何代码优化。
*   **强制 `pprof`**: 在进行性能调优前，**必须**使用 `pprof` 进行 CPU、内存、阻塞分析。
*   **强制 Benchmark**: **必须**为性能敏感的代码路径编写 Benchmark 测试，以量化优化效果。

## 2. 内存分配优化

*   **预分配**: 当能够预估容量时，**必须**使用 `make` 的 `capacity` 参数来创建 slice 和 map。
*   **字符串拼接**: **严禁**在循环或频繁调用的函数中使用 `+` 拼接字符串，**必须**使用 `strings.Builder`。
*   **对象复用**: 对于频繁创建和销毁的大对象，应使用 `sync.Pool` 进行复用。
*   **指针 vs 值**: 当 `struct` 体积较大（> 64字节）或包含不可复制字段时，**必须**使用指针传递。

## 3. 常见性能模式

*   **`defer`**: 在极度性能敏感的热点路径（如循环内部），应避免使用 `defer`。在其他场景，**必须**使用 `defer` 以保证健壮性。
*   **正则表达式**: **必须**在包级别使用 `regexp.MustCompile` 预编译正则表达式，**严禁**在函数或循环内部编译。
*   **原子操作**: 对于简单计数器等，**必须**优先使用 `sync/atomic` 而不是 `sync.Mutex`。
