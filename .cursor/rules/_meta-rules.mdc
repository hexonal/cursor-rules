---
description: "全局元规则 (v1.0)，包含所有规范的最高优先级原则。"
globs: 
alwaysApply: true
---
# Go 编程全局元规则 (v1.0)

本文件包含的规则具有**最高优先级**，其效力凌驾于所有其他 `.mdc` 规范文件之上。

## 1. 最高指令：最小化变更原则 (Principle of Minimum Change)

*   **核心规则**: 在执行任何代码修改、重构或审查任务时，最优先的原则是**“最小化必要变更”**。
*   **强制**:
    1.  **严禁**对任何未被明确要求修改的代码部分进行任何形式的风格调整、变量重命名、逻辑重构或“优化”。
    2.  代码的修改**必须**仅限于：
        *   实现用户提出的具体需求。
        *   修正现有代码中与本规范体系内某条**“必须 (MUST)”**或**“严禁 (FORBIDDEN)”**条款明确冲突的部分。
*   **目的**: 此规则旨在防止 AI 或开发者引入不必要的、未经充分测试的、或可能破坏原有逻辑稳定性的“过度优化”和“个人风格偏好”。保持代码的稳定性和可追溯性是首要任务。

## 2. 最终解释权：工具标准

*   **格式化的最终标准**: `goimports` 工具的输出是所有关于代码格式化、`import` 语句分组和排序风格的**唯一和最终标准**。
*   **规范的兼容性**: 本规范体系中的任何条款，如果其解释可能与 `goimports` 的默认行为产生冲突，均以 `goimports` 的行为为准。
*   **禁止**: **严禁**手动修改由 `goimports` 生成的任何格式。
