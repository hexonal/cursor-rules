---
description: "Go 项目演进与生命周期规范 (v2.1)，定义了特性门控和功能生命周期管理（Alpha, Beta, GA）的强制标准。"
globs: 
alwaysApply: true
---
# Go 项目演进与生命周期规范 (v2.1)

> **注意**: 本规范之上存在一份 [全局元规则](mdc:_meta-rules.mdc)，其具有最高解释权。

本规范旨在为需要长期维护和迭代的 Go 项目提供一套完整的、无二义性的功能演进与生命周期管理标准。

## 1. 核心机制：特性门控 (Feature Gates)

*   **强制**: 所有对用户可见的、有影响力的新功能，或任何可能引入破坏性变更的修改，**必须**通过特性门控机制进行管理。
*   **实现**:
    1.  项目**必须**提供一个全局的、并发安全的特性门控注册表。
    2.  每个受控功能**必须**关联一个唯一的、字符串类型的门控键。
    3.  新功能的代码逻辑**必须**被 `if featuregate.IsEnabled("...")` 条件块包裹。
    4.  特性门控的状态**必须**可通过应用的配置进行控制。

## 2. 强制规范：特性生命周期 (Feature Lifecycle)

所有通过特性门控管理的功能，**必须**遵循以下生命周期阶段。

### 2.1. Alpha

*   **定义**: 实验性功能。
*   **强制规则**:
    1.  **默认关闭**: **必须**在应用默认配置下处于禁用状态。
    2.  **无兼容性保证**: 其 API 和行为可以在任何版本中被不兼容地修改或彻底移除，无需遵循废弃策略。
    3.  **文档**: **必须**在文档中明确标注为 `Alpha` 阶段。

### 2.2. Beta

*   **定义**: 趋于稳定的功能。
*   **强制规则**:
    1.  **默认开启**: **必须**在应用默认配置下处于启用状态。
    2.  **可禁用**: **必须**提供选项让用户可以显式禁用该功能。
    3.  **废弃策略**: 如果决定移除或进行不兼容的修改，**必须**遵循[废弃策略](mdc:go-comments.mdc#4-特殊标记)。

### 2.3. GA (General Availability / Stable)

*   **定义**: 官方支持的、生产就绪的稳定功能。
*   **强制规则**:
    1.  **强制开启**: **必须**从特性门控中移除该功能的控制逻辑，使其成为项目的永久组成部分。
    2.  **兼容性承诺**: **必须**遵循项目的语义化版本和向后兼容性承诺。
    3.  **废弃策略**: 移除或修改 GA 功能**必须**经过严格的、多版本的废弃流程。
