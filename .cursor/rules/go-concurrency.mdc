---
description: 
globs: 
alwaysApply: true
---
# Go 并发编程规范 (v2.1)

> **注意**: 本规范之上存在一份 [全局元规则](mdc:_meta-rules.mdc)，其具有最高解释权。

本规范旨在提供一套完整的、无二义性的 Go 并发编程标准。

## 1. Goroutine 生命周期管理

*   **原则**: 任何被启动的 Goroutine，都**必须**有明确的、可预测的退出机制。
*   **强制**:
    1.  **可取消**: 任何可能阻塞或长时间运行的 Goroutine，其入口函数**必须**接受 `context.Context` 作为第一个参数，并**必须**在内部使用 `select` 语句监听 `ctx.Done()` 以响应取消信号。
    2.  **可等待**: 当一个 Goroutine 启动了多个子 Goroutine 时，**必须**使用 `sync.WaitGroup` 来等待所有子 Goroutine 执行完毕。
    3.  **禁止孤儿 Goroutine**: **严禁**创建无法被父级 Goroutine 控制或等待其完成的 Goroutine。

## 2. 同步原语

*   **互斥锁 (`sync.Mutex`)**:
    *   **最小化临界区**: **必须**将 `Lock()` 和 `Unlock()` 调用之间的代码量减至最少。耗时操作（如 I/O）**必须**在锁区域之外执行。
    *   **`defer`**: **必须**使用 `defer mu.Unlock()` 来确保锁的释放。
*   **读写锁 (`sync.RWMutex`)**:
    *   **使用场景**: **仅当**读操作远多于写操作时，才应使用 `RWMutex`。否则，**必须**使用 `sync.Mutex`。
*   **并发度控制**:
    *   对于会消耗有限资源的并发操作，**必须**控制其并发数量（如使用 Worker Pool 或带缓冲 Channel）。
*   **死锁避免**:
    *   如果需要获取多个锁，**必须**保证所有 Goroutine 都以**相同的顺序**获取这些锁。
    *   **必须**在 CI 流程中加入 `go test -race` 进行数据竞争检测。
